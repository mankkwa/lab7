package client;

/**
 * Перечисление существующих команд
 */
public enum CommandType {
    ADD,
    CLEAR,
    UPDATE,
    EXIT,
    SAVE,
    SHOW,
    REMOVE_BY_ID,
    EXECUTE_SCRIPT,
    REMOVE_GREATER,
    HELP,
    REMOVE_FIRST,
    AVERAGE_OF_ANNUAL_TURNOVER,
    PRINT_UNIQUE_FULL_NAME,
    COUNT_GREATER_THAN_POSTAL_ADDRESS;
}
